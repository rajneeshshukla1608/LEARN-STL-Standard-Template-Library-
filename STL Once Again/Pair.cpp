#include<bits/stdc++.h>
using namespace std;

// Pair here - Pair does not works as map key and value pairs:

// pair<int, int> f(){
//     return {3, 4};
// }

// int main(){
    // pair<int, int>; // a pair of 2 ints;

//     pair<int, string> p = {4, "raj"}; // A pair of an int aand a stirng
//     p.first = 13; // You can change the values like this

//     cout << p.first << " " << p.second; // print partially elements
    //    pair<int, pair<int, string>> p; // A pair of int and (pair of int and string);
    //    pair<vector<int>, string> p;   // a pair of a(vector of int) and a string;
//     return 0;
// }

//
// ******* Sorting arrays/vectors of pairs(very useful)
int main(){
    // pair<int, int> p[2];
    // p[0] = {1, 2};
    // p[1] = {5, 2};

    // sort(p, p + 2);
    vector<pair<int, string>> v(2);
    

}